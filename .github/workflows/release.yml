name: Release

on:
  push:
    tags:
      - 'v*'

env:
  RUSTFLAGS: --allow=unexpected_cfgs

jobs:
  build:
    name: Build (${{ matrix.target }})
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            suffix: linux-x86_64
          - target: aarch64-apple-darwin
            os: macos-latest
            suffix: macos-aarch64
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            suffix: windows-x86_64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build --release --workspace
      - name: Upload CLI
        uses: actions/upload-artifact@v4
        with:
          name: arithma-${{ matrix.suffix }}${{ matrix.os == 'windows-latest' && '.exe' || '' }}
          path: target/release/arithma${{ matrix.os == 'windows-latest' && '.exe' || '' }}
      - name: Upload MCP server
        uses: actions/upload-artifact@v4
        with:
          name: arithma-mcp-${{ matrix.suffix }}${{ matrix.os == 'windows-latest' && '.exe' || '' }}
          path: target/release/arithma-mcp${{ matrix.os == 'windows-latest' && '.exe' || '' }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/**/*
